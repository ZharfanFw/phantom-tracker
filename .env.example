# Habit Tracker Server Environment Variables
PORT=5050
NODE_ENV=development

# Database connection (PostgreSQL)
DATABASE_URL=postgres://postgres:postgres@localhost:5435/habit_tracker

# Optional API key for remote access / widget security if enabled
API_SECRET_KEY=
